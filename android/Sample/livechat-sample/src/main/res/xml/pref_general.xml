<PreferenceScreen xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!--SwitchPreference
        android:defaultValue="true"
        android:key="example_switch"
        android:summary="@string/pref_description_social_recommendations"
        android:title="@string/pref_title_social_recommendations" /-->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:inputType="textCapWords"
        android:key="turn_url"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Turn url" />

    <EditTextPreference
        android:capitalize="words"
        android:inputType="textCapWords"
        android:key="turn_user"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Turn username" />

    <EditTextPreference
        android:capitalize="words"
        android:inputType="textCapWords"
        android:key="turn_pass"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Turn password" />

    <EditTextPreference
        android:capitalize="words"
        android:inputType="textCapWords"
        android:key="connect_addr"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Connect address" />


    <ListPreference
        android:defaultValue="1"
        android:entries="@array/modes"
        android:entryValues="@array/modes"
        android:key="mode"
        android:title="Mode" />

    <ListPreference
        android:defaultValue="1"
        android:entries="@array/logmodes"
        android:entryValues="@array/logmodes"
        android:key="logmode"
        android:title="Log mode" />


    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:key="ice_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="ice servers" /-->

</PreferenceScreen>
